#include <gtest/gtest.h>
#include <opencv2/opencv.hpp>
#include <vector>
#include <gtest/gtest.h>
#include <chrono>
#include <thread>

TEST(PLUGINS, Track) {
	//TODO test track plugins
	std::this_thread::sleep_for(std::chrono::microseconds(10));
}